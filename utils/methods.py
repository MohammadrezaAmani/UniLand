def send_message_to_addmin(user_id, message):
    """send user messages to addmin

    Args:
        user_id (int): user id
        message (str): text of messages

    Returns:
        bool: sent or error
    """
    return True