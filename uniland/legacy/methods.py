async def send_message_to_admin(client, message):
    print("send_message_to_admin")
    """send user messages to addmin

    Args:
        user_id (int): user id
        message (str): text of messages

    Returns:
        bool: sent or error
    """
    return True
